function doubleNumber(number){
    console.log('Classwork day 4')
    let number=promt("Enter a number")
    if(number= null){
        console.log('Enter a number')
    }
    else if(isNAN(number)){
        console.log('Enter a number')
    }
    else
    number *= 2
    return number
}
doubleNumber();


//when the function doubleNumber is called and the user didn't enter an argument, number as an argument, the program will ask the user to enter a number. 
//the function will fully run when the user enters an argument.

